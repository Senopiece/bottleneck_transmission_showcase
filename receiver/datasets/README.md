# Datasets

Dataset area for raw recordings and generated artifacts.

Raw videos live under `raw/videos/good` and `raw/videos/bad`.
Generated overlays, pseudo labels, crops, traces, and metrics live under `derived`.
