# Run Tracker Dataset

Runs the acquire/track loop over all raw videos, then writes:

- per-video CSV metrics;
- overlay videos;
- a summary CSV.

The current backend is a stub. It is a stable harness for the future C++ tracker.
