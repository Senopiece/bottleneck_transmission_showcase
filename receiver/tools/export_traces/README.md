# Export Traces

Future scripts for neural BP datasets.

This directory is not used by the current tracker/LED reader training loop.
When neural BP work starts, these scripts should export:

- LED score and packet LLR time series from Android/debug logs;
- fountain/LDGM measurement ids;
- BP posterior snapshots;
- decode success/failure metadata.
