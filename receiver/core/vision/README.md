# Vision Core

Future C++ acquirer/tracker/LED-reader implementation.

The core should not depend on Android, iOS, OpenCV, or Python. Platform/tool code should decode frames and provide a `FrameView`.
